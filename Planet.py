class Earth:
    def next_year_update(self):
        self.now_people = len(self.people) * n
        self.now_people += (self.now_people) / 80
        self.new_groups = (self.now_people / n) - len(self.people)
        for i in range(self.new_groups):
            x = random(-1000, 1000)
            y = random(-1000, 1000)
            self.people.append(x, y)